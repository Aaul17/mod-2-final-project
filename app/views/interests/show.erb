<h2>You are logged in as <%=link_to current_user.username, current_user %></h2>
<%= button_to "Logout", logout_path, method: :delete %>
<br>

<h1><%= @interest.name %></h1>

<p>Pigeons that hoot at this activity:</p>
<ul>
  <% @interest.users.each do |user| %>
  <li><%= link_to user.username, user %></li>
  <% end %>
</ul>
<br>

<%= button_to "View All Interests", interests_path, method: :get %>
