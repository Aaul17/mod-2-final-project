<%= form_for(current_user.active_relationships.find_by(matched_id: @user.id), method: :delete) do |f| %>
  <%= f.submit "Unmatch", data: { confirm: "You no longer want to flock with this bird?"} %>
<% end %>
